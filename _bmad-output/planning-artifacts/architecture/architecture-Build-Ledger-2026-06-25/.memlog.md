---
scope: Build Ledger v1: Collector (Python) + Generated Page, the seven evidence Modules, and the build-ledger.json contract coupling them; safe-by-default redaction over ~35 private client repos. Reconciles the existing v0.2 collector spike up to the PRD's locked contract.
purpose: build-substrate + evidence — terse build contract for agentic dev and downstream BMAD epics/stories, AND inspectable material for the Build Ledger page itself (judgment-on-display for the GL Window).
altitude: initiative (keeps the seven Modules/features coherent)
updated: 2026-06-25T13:07
---

- (direction by user) Working mode = Coaching. Deliverable = terse build-substrate spine PLUS the architecture rendered as inspectable evidence for the Build Ledger page itself (on-thesis: the artifact demonstrates the skill it documents). Implication: judgment calls + trade-offs must be crisp and inspectable.
- (constraint) [ADOPTED — PRD FR-8/API Contracts] build-ledger.json is the SINGLE contract between Collector and Generated Page; every Module emits into it; it carries schema_version; the page renders SOLELY from it (removing the collector does not change what a given file can display).
- (constraint) [ADOPTED — PRD FR-6] Redaction is safe-by-default + fail-closed: every private repo defaults to display_tier=aggregate_only (Silhouette); a whole-document redaction assertion runs over the ENTIRE public file before publish; a run that cannot assert clean does not publish. Allowlist is the ONLY promotion mechanism above aggregate_only and ships EMPTY in v1.
- (constraint) [ADOPTED — PRD FR-6/FR-11] mirror_view NEVER appears in the public build-ledger.json: public retrospective carries window_view ONLY; the brutally-honest mirror_view is written to a separate private/mirror.json that never publishes and is never linked. Machine-checkable: published file has no retrospective.mirror_view key.
- (constraint) [ADOPTED — PRD Versioning] schema_version is semver MAJOR.MINOR.PATCH. Additive (new optional keys) = MINOR and must not break a page built for an earlier MINOR; the forward-compatible optional 'attribution' rep lands v1.5 line-level as MINOR (->1.1.0). Breaking (rename/remove load-bearing key, change meaning, tighten redaction non-back-compat) = MAJOR; the page declares the MAJOR it supports and refuses an unsupported MAJOR.
- (constraint) [ADOPTED — PRD §6.1.1] v1 ships commit-level Co-Authorship Split ONLY, stated as an explicit lower bound (one of four Attribution Layers). Line-level Attribution (FR-13, session-diff reconcile) + Acceptance Ratio (FR-5) are v1.5 under a MINOR bump. No raw transcript/session-log text EVER enters the public file or page — aggregate counts/ratios only.
- (constraint) [ADOPTED — PRD §5/Aesthetic] Provenance-first presentation: hero leads with Co-Authorship Split + AI-Native Artefacts; volume (repos/commits/LOC) is supporting cast, never headlined. Rejected in ALL versions: cost/token leaderboards, Wrapped-style spend recap, desk-toy spend counters. Acceptance Ratio (offered-vs-kept) is the sanctioned quality signal, itself not a maximization target.
- (constraint) [EXISTING REALITY — collector/collect.py v0.2.0] Python spike, working end-to-end: gh CLI for repo list, local clone scan in ~/Projects, central redaction + hard leak assertion, emits public/build-ledger.json (47 repos: 12 public + 35 silhouettes; 61.4% AI-co-authored). DIVERGES from locked contract: string schema_version 'build-ledger.v1' (vs semver 1.0.0); 'visibility' (vs display_tier + allowlisted); flat ai_artefacts list (vs 3 Artefact Classes); exclusions-as-string-list (vs counts); signals shape differs. Spike convergence to the contract is a spine concern.
- (event) Activation complete; inherited PRD constraints logged; entering coaching on open calls.
- (decision by user) [AD candidate — Runtime model = LOCAL-PRIMARY (chosen by Raedmund; 'I can't rely on GitHub Actions')]. Binds: all Modules, FR-7 (schedule), FR-6 (redaction surface), publish step of FR-9. Prevents: private client code reaching third-party CI; modules silently unavailable in the canonical cut; divergent run environments. Rule: the Collector runs ONLY on a trusted local machine that holds the repo clones + agent logs + BMAD memlogs; it is the SOLE producer of public/build-ledger.json AND private/mirror.json. Weekly cadence = a LOCAL scheduler (launchd/cron), not GitHub Actions. Private repository contents/history and local agent logs MUST NOT be transmitted to any third-party CI runner. The ONLY artifact that may leave the trusted machine is the post-redaction public build-ledger.json (+ the page derived from it). Any CI is limited to serving/deploying the static page from the committed public JSON and never has repo or log access. Privacy posture is itself judgment-on-display for the Window: 'client code never leaves my machine; only redacted aggregates are published.'
- (version) [VERIFIED web 2026-06-25] Astro 6.0 (released 2026-03-10) is current + the 2026 default for content/data-driven static sites; zero-JS-by-default (HTML-first, islands only for interactive bits) = ideal inspectable work-sample. Cloudflare acquired Astro (Jan 2026) -> Cloudflare Pages is first-class + local-deployable via wrangler (no GitHub Actions).
- (decision) [AD candidate — Presentation layer = Astro 6, BUILD-TIME render]. Binds: FR-2/FR-9/FR-10 (Generated Page), the publish step. Prevents: page+publish diverging on how the JSON is consumed; a JS-bundle inspection surface on a graded work-sample. Rule: the Generated Page is rendered AT BUILD TIME from build-ledger.json into static HTML/CSS/SVG (charts = server-generated inline SVG, no charting lib; islands only where genuinely interactive). The published build-ledger.json is served beside the page (FR-9) so a reader can diff page-against-file. v1 site = a MINIMAL Astro shell + the /engineering page only; broader raedmund.com is out of v1 scope. Collector(Python)->build-ledger.json(contract)->Astro build is a clean two-language split ON the filter boundary.
- (direction by user) Hard constraint: MINIMAL COST. Binds all infra/hosting/runtime choices to /bin/zsh / free-tier, no backend, no paid CI. (Collector already local = /bin/zsh.)
- (decision by user) [AD candidate — Repo topology = ONE PUBLIC REPO (chosen by Raedmund, Option 1)]. The single repo holds Collector (Python) + Astro site + the published (already-redacted) build-ledger.json — doubling as the open-methodology work sample (the Window can audit exactly how figures are computed/redacted; auditability proven not asserted). Binds: FR-6 privacy, the whole project layout. Prevents: private data ever entering a publishable tree. Rule (privacy-structural, by construction not by gitignore-discipline): the Collector MUST write every private output (private/mirror.json, raw evidence CSVs, any per-client intermediate) to a path OUTSIDE the repo tree (a local 'locked drawer', e.g. ~/.build-ledger/private/), and reads repo clones from ~/Projects (external) — so the repo CANNOT contain private data by construction. Defense-in-depth: an in-repo private/ path is also gitignored. The ONLY private->public crossing is the single redacted build-ledger.json after its fail-closed assert. The current ~/Projects/Build-Ledger dir (not yet a git repo) becomes this repo.
- (decision) [AD candidate — Publish path + hosting]. Hosting INVARIANT: a static host, free tier, deployed PRE-BUILT from local (no host build minutes, no GitHub Actions, /bin/zsh). Specific host = SEED/swappable (dist/ deploys identically anywhere); recommended default = Cloudflare Pages free [VERIFIED 2026-06-25: unlimited bandwidth, no egress, 5 custom domains, deploy via 'wrangler pages deploy ./dist']; Vercel rejected for this page (Hobby tier is non-commercial-use only -> a job-seeking page is a gray-zone violation; 100GB hard cap). raedmund.com = existing domain -> /engineering route. Weekly LOCAL pipeline (cron/launchd): (1) Collector emits redacted build-ledger.json into repo + private outputs to the drawer; (2) astro build -> dist/; (3) wrangler pages deploy; (4) git commit build-ledger.json + push (its git history = a bonus provenance trail of the ledger's own evolution). Secrets: the Pinstack GitHub token lives ONLY in local gh/env auth — never in any repo or CI (a free simplification of local-primary collection).
- (decision by user) [ADOPTED tail — contract authority] The PRD's build-ledger.json shape is the authoritative single source of truth; Collector + Page both conform; the v0.2 spike converges to it (deltas = build punch-list: string schema_version -> semver 1.0.0; visibility -> display_tier+allowlisted; flat ai_artefacts list -> 3 Artefact Classes; exclusions list -> counts).
- (decision by user) [ADOPTED tail — module ownership] Each of the 7 Modules owns exactly ONE top-level key; the Collector is the SOLE writer; no Module reads/writes another's key; a Module with no data this run emits available:false explicitly (never silently omitted). [PRD FR-8 module-availability]
- (decision by user) [ADOPTED tail — cost-source, resolves PRD open-Q1] Cost figures come from what Claude Code/Codex logs already record; where unrecorded, from a PINNED, DATED, published price table shipped in-repo (with source link); the price source + as-of date are shown on the page; never estimated/invented; an unsourceable figure is OMITTED, not guessed. [binds FR-5, SM-C1]
- (direction by user) [DEFERRED tail] Representative-systems framing (Meshic/geo-ingest/mcp-toolbelt named section) = page-design/UX decision (PRD open-Q2), NOT a spine invariant; addable later as an optional key via MINOR bump. Exact config/*.yml contents = structural seed, set at distill.
- (event) Raedmund approved the tail (Proceed). All decisions locked; distilling the spine.
- (event) Reviewer Gate ran: lint CLEAN (0); 6 parallel subagents (reconcile PRD/addendum/spike + rubric + tech-currency + adversarial). Verdicts: spine faithful & terse, all FR-1..13 land, tech verified-current; adversarial found 3 CRITICAL ownership holes. Reviews in reviews/.
- (decision) [AD-13 NEW — from adversarial gate] Collector assembles shared collections; Modules contribute by id. Binds repositories[]; FR-1/3/4. Prevents two Modules minting mismatched-length/order repo lists on an unowned id. Rule: collect.py owns+assembles the repositories[] array and the stable per-row id; per-repo Modules (repos/coauthorship/artefacts) return id-keyed maps of their own field-path, merged by id. One writer per FIELD-PATH, one owner of the array, one source of id.
- (decision) [AD-14 NEW — from adversarial gate] Aggregates are a derived projection, never hand-written. Binds aggregates; FR-1/2. Prevents headline totals that don't reconcile with drilled-into rows. Rule: aggregates computed by the assembler as a pure function of already-emitted repositories[] after all per-repo Modules contribute; no Module writes aggregates; each figure must equal the sum/derivation of the rows it summarizes (reconcilable by construction).
- (decision) [AD-15 NEW — from adversarial gate] One atomic, deterministic, fail-closed run. Binds FR-7/8/6; publish. Prevents half-old/torn published file + non-reproducible churn + redaction-before-aggregation. Rule: build the whole document in memory in fixed Module order, then atomic-replace write. Pinned order: tiering (assign display_tier) -> per-repo Module contributions -> aggregation (AD-14) -> whole-doc redaction assert (AD-4, final, mutates nothing) -> atomic write. Degraded Module sets available:false and run continues; fatal error (assert fail / not schema-valid) aborts publish (fail-closed). Deterministic output so the committed JSON diff = real change not churn.
- (decision) [AMEND AD-3] ownership is per-SLICE not per-top-level-key (a whole key OR a named field-path in a shared collection, per AD-13/14); empty = available:false with TYPED-empty sub-objects matching the populated shape (no bare {}/[]). [AMEND AD-4] display_tier is a fixed 3-value enum (public|redacted|aggregate_only; redacted defined+representable but unused in v1); free-text fields constrained to controlled-vocabulary/aggregate-ints by construction (allowlist), the whole-doc assert is the fail-closed backstop (a denylist can't catch a novel client name). [AMEND AD-1] explicitly supersedes the PRD's weekly-GitHub-Action mechanism; token is a local secret. [AMEND AD-10] govern the contract SHAPE; page emphasis is a presentation convention PRD/UX owns.
- (direction) [Handoff flag] The PRD now trails the spine on: GitHub-Action->local override (FR-7, Platform, Safety 'token held by the Action'), and the added pricing.yml. Offer to reconcile the PRD so upstream + spine don't silently diverge.
- (event) Gate fixes applied to spine (AD-13/14/15 added; AD-1/3/4/10 amended; conventions/stack/capability-map/punch-list updated). Re-lint CLEAN (0 findings). 15 ADs, ascending, no dupes; 2 valid mermaid diagrams.
- (event) Rendering produced (purpose = 'it's also evidence'): architecture-evidence.html — self-contained, on-brand (restrained/numerical/monospace-caps/muted-accent), foregrounds the 3 judgment forks + 15 ADs + the pipeline/privacy-boundary SVG. Hostable as raedmund.com/engineering/architecture.
- (event) spine finalized (status=final, 2026-06-25).
- (event) Run closed; recommending bmad-spec adoption next + PRD reconciliation handoff.
